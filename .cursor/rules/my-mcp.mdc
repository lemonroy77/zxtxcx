---
description: Cursor聊天插件通信规则 - 用户通过外部桌面应用发消息，AI 通过 check_messages 收发
globs:
alwaysApply: true
---

# Cursor聊天插件规则

用户使用独立桌面应用「Cursor聊天插件」发送消息，AI 通过 `check_messages` 工具接收和回复。

## 如何开始

1. 用户在 Cursor 对话中输入: `请使用 my-mcp-N 的 check_messages`（N 为会话编号）
2. AI 调用对应 MCP 的 `check_messages()` 工具进入等待
3. 用户在插件中发送消息，AI 会收到并回复

## 核心规则

1. **先正常对话**：收到消息后像普通聊天一样直接回应（问候、问答、执行任务）
2. **回复方式**：所有回复内容通过 `reply` 参数发送到插件。**不要在 Cursor 对话窗口重复输出回复文本**，避免用户看到两份一样的消息
3. **Cursor 窗口**：只用于显示工具调用、代码修改等操作过程，不输出对用户的回复文字
4. **保持循环**：回复后**必须立刻**再调用 `check_messages()` 等待下一条消息
5. 插件每次只推一条消息，连发多条会分多次送达

## reply 参数用法

- `reply` 是字符串参数，内容会显示在插件的聊天界面中
- **必须始终传 reply**：用户主要通过插件看回复，不传 reply 等于没回复
- **reply 只发简短摘要**（1-3句话），长文本、代码、详细说明写在 Cursor 窗口
- 执行代码任务时，reply 写进度和结果摘要
- **禁止在 reply 中发送超过500字的内容**，长内容会导致插件卡顿

## ⚠️ 绝对不能断开连接

- **任务完成后必须继续调用 `check_messages()`** 挂起等待下一条消息
- **MCP 超时后必须立刻重新调用 `check_messages()`** 重新挂起
- **任何情况下都不能让会话断开** —— 用户随时可能发新消息
- 把 `check_messages()` 当作永远在线的消息监听器，只要对话还在就必须保持调用

## 禁止

- ❌ 用「工具正常」「通道已验证」等套话代替回复
- ❌ 长篇解释 MCP/插件原理
- ❌ **完成任务后不调用 `check_messages` 就结束**（等于中断用户）
- ❌ 等待用户在 Cursor 里打字（用户从插件发消息）
- ❌ 以任何理由终止 check_messages 循环

## 流程（永久循环）

```
check_messages() → 收到消息 → 回复(reply=摘要) → check_messages() → 等待…
↑ 超时/出错 → 立刻重新调用 check_messages() ↑
```

## 多会话

最多 **32** 路并行会话。每个 Cursor 对话窗口绑定一个 `my-mcp-N`。
